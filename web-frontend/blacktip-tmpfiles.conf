# Tmpfiles configuration for Blacktip services
# This ensures runtime directories are created with correct permissions on boot

# Runtime directory for Gunicorn pid files
d /run/blacktip 0755 blacktip blacktip -
